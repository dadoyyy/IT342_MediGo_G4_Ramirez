feat: role selection step before registration form

Register page now has two steps:

Step 1 - Role selection:
- Two large clickable cards: Patient and Doctor
- Each card shows icon, title, and description
- Google OAuth button also available at this step
- Animated transition to step 2 on selection

Step 2 - Registration form:
- Back button returns to role selection
- Role badge shown in header (e.g. "Patient" or "Doctor")
- Form sends the selected role to the backend
- Doctors are redirected to /doctor/register after account
  creation to complete their professional profile
- Patients are redirected to /login after registration
